sudo: required
services:
  - docker

before_install:
  - docker build -t sunnatulloganiev/docker-react -f Dockerfile.dev .

script:
  - docker run sunnatulloganiev/docker-react npm run test -- --coverage
